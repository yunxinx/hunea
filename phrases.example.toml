# mode = "append" 保留内置短语并追加自定义短语。
# mode = "override" 仅使用下方列出的短语。
mode = "append"

# order = "random" 每次请求随机选择一条短语。
# order = "cycle" 按顺序依次循环使用短语。
order = "random"

phrases = [
  "Reading context",
  "Thinking through it",
  "Checking the edges",
]
